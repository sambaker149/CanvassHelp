@page
@model CanvassHelp.Pages.InputModel

@{
    ViewData["Title"] = "Record Your Conversations";
}

<div class="text-center">
    <h1 class="display-4">CanvassHelp Web Application</h1>
    <h2>Record Your Conversations</h2>
</div>

@*<table class="table text-center">
    <thead>
        <tr>
            <th>
                <h5>Resident ID</h5>
            </th>
            <th>
                <h5>Name</h5>
            </th>
            <th>
                <h5>Email</h5>
            </th>
            <th>
                <h5>Mobile</h5>
            </th>
            <th>
                <h5>Last Contacted</h5>
            </th>
            <th>
                <h5>Last Voting Intention</h5>
            </th>
            <th>
                <h5>Contact Preference</h5>
            </th>
            <th>
                <h5>Address</h5>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Resident)
        {
            <tr>
                <h3>@Html.DisplayFor(modelItem => item.FirstNames) @Html.DisplayFor(modelItem => item.LastNames)</h3>
                <h6>
                    <b>Address: </b>
                    @Html.DisplayFor(modelItem => item.Address.HouseNumber)
                    @Html.DisplayFor(modelItem => item.Address.StreetName)
                    @Html.DisplayFor(modelItem => item.Address.Postcode)
                </h6>
                <h6><b>Last Contacted: </b>@Html.DisplayFor(modelItem => item.LastContacted)</h6>
                <h6><b>Last Voting Intention: </b>@Html.DisplayFor(modelItem => item.VoteIntention)</h6>
                <h6><b>Contact Preference: </b>@Html.DisplayFor(modelItem => item.ContactPreference)</h6>
                <a class="btn btn-primary shadow" role="button" asp-page="./ResidentEdit" asp-route-id="@item.ResidentId" 
                style="background: #567b92;border-style: none;border-color: var(--bs-navbar-hover-color);">Record Conversation</a>
            </tr>
            <br />
            <br />
        }
    </tbody>
</table>*@
<div class="album py-5 bg-light">
    <div class="container">
        <div class="row">
            @foreach (var voter in Model.Resident)
            {
                <div class="col-md-6">
                    <div class="card mb-6 box-shadow">
                        <div class="card-body">
                            <h4 class="card-text">@voter.FirstNames @voter.LastNames</h4>
                            <h6 class="card-text"><b>Address: </b>
                                @voter.Address.HouseNumber 
                                @voter.Address.StreetName 
                                @voter.Address.Postcode
                            </h6>
                            <h6 class="card-text"><b>Last Contacted: </b>@voter.LastContacted</h6>
                            <h6 class="card-text"><b>Last Voting Intention: </b>@voter.VoteIntention</h6>
                            <h6 class="card-text"><b>Contact Preference: </b>@voter.ContactPreference</h6>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                    <a class="btn btn-primary shadow" role="button" asp-page="./ResidentEdit" asp-route-id="@voter.ResidentId"
                                       style="background: #567b92;border-style: none;border-color: var(--bs-navbar-hover-color);margin: 5px">Record Conversation</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

